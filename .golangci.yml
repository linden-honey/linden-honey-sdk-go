linters-settings:
  goimports:
    # put imports beginning with prefix after 3rd-party packages;
    # it's a comma-separated list of prefixes
    local-prefixes: github.com/linden-honey/linden-honey-sdk-go

linters:
  enable:
    - gofmt
    - goimports
